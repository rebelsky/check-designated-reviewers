# data files (input and output); we don't want the data uploaded
*.tsv
*.xlsx
*.numbers

# .html files are generated by the program; we don't want them uploaded, either
*.html

# Temporary files from various editors.
*.swp

# A place to store things we don't want uploaded
private

# The joys of macOS
.DS_Store
